name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
      - "feat/**"
      - "fix/**"
      - "chore/**"

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  gateway:
    name: Gateway / Rust
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: gateway

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Install declared minimum Rust toolchain
        run: |
          rustup toolchain install 1.94.0 --profile minimal --component clippy,rustfmt
          rustup override set 1.94.0

      - name: Cache Cargo dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry/cache
            ~/.cargo/registry/index
            ~/.cargo/git/db
          key: ${{ runner.os }}-cargo-1.94.0-${{ hashFiles('gateway/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-1.94.0-

      - name: Check formatting
        run: cargo fmt --all --check

      - name: Run Clippy
        run: cargo clippy --locked --all-targets -- -D warnings

      - name: Run tests
        run: cargo test --locked

  analytics:
    name: Analytics / Java
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: analytics

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Install Java 25
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "25"
          cache: maven

      - name: Compile and run tests with warnings denied
        run: mvn --batch-mode test package

  migrations:
    name: Database / Migrations
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Prepare CI environment
        run: cp .env.example .env

      - name: Validate shell scripts
        run: bash -n infra/postgres/init/01-roles.sh infra/minio/bootstrap.sh

      - name: Validate Compose configuration
        run: docker compose config --quiet

      - name: Start Postgres
        run: docker compose up -d --wait postgres

      - name: Apply all migrations
        run: docker compose run --rm migrator

      - name: Roll back all migrations
        run: >-
          docker compose run --rm migrator
          -path=/migrations
          -database="postgres://buildlens:buildlens_dev_password@postgres:5432/buildlens?sslmode=disable"
          down -all

      - name: Reapply all migrations
        run: docker compose run --rm migrator

      - name: Verify schema version
        run: |
          version="$(docker compose exec -T postgres psql -U buildlens -d buildlens -Atc 'SELECT version FROM schema_migrations')"
          test "$version" = "11"

      - name: Stop migration services
        if: always()
        run: docker compose down -v

  stack:
    name: Stack / Smoke Test
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Prepare CI environment
        run: cp .env.example .env

      - name: Start application dependencies
        run: docker compose up -d --wait postgres redis rabbitmq minio

      - name: Apply migrations
        run: docker compose run --rm migrator

      - name: Create object-storage buckets
        run: docker compose run --rm minio-init

      - name: Build and start application images
        run: docker compose --profile app up -d --build --wait --no-deps gateway analytics

      - name: Check liveness and readiness
        run: |
          curl --fail --silent --show-error http://localhost:8080/health
          curl --fail --silent --show-error http://localhost:8080/health/ready
          curl --fail --silent --show-error http://localhost:8081/actuator/health

      - name: Show application logs after failure
        if: failure()
        run: docker compose --profile app logs gateway analytics

      - name: Stop application services
        if: always()
        run: docker compose --profile app down -v

# RabbitMQ

No topology here yet. Exchanges, queues, bindings and the dead-letter policy are
defined in **Phase 4**, alongside the event contracts in `/contracts`. They are
two halves of one decision and land together.

For now the container just runs with the management UI enabled, so you can watch
an empty broker at http://localhost:15672.

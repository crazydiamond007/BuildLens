# BuildLens local development stack.
#
#   make up      infrastructure only (postgres, redis, rabbitmq, minio) + migrations
#   make up-all  the above plus all application containers (profile: app)
#   make dev     infrastructure in docker, gateway on the host via cargo run
#
# Every dependency has a healthcheck, so `depends_on: condition: service_healthy`
# actually means "ready", not merely "process started". The migrator runs to
# completion before any application service is allowed to start.

name: buildlens

services:
  postgres:
    # 18, not 16: the schema uses the native uuidv7() added in 18. See
    # infra/migrations/000001_init.up.sql.
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      # Consumed by init/01-roles.sh, which runs once on an empty data volume.
      GATEWAY_DB_PASSWORD: ${GATEWAY_DB_PASSWORD}
      ANALYTICS_DB_PASSWORD: ${ANALYTICS_DB_PASSWORD}
      AI_DB_PASSWORD: ${AI_DB_PASSWORD}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      # Mounted at /var/lib/postgresql, NOT /var/lib/postgresql/data. Postgres 18
      # keeps its data in a major-version subdirectory (.../18/docker) so that
      # pg_upgrade can see both versions across one mount point. The image
      # refuses to start if it finds a volume at the old path.
      - postgres_data:/var/lib/postgresql
      - ./infra/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    restart: unless-stopped

  # Runs the SQL in infra/migrations to completion, then exits 0. This is the
  # only thing in the system permitted to change the schema. See AGENTS.md.
  migrator:
    image: migrate/migrate:v4.17.1
    volumes:
      - ./infra/migrations:/migrations:ro
    command:
      - -path=/migrations
      - -database=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      - up
    depends_on:
      postgres:
        condition: service_healthy
    restart: on-failure

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "${REDIS_PORT}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped

  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_DEFAULT_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_DEFAULT_PASS}
      RABBITMQ_DEFAULT_VHOST: ${RABBITMQ_DEFAULT_VHOST}
    ports:
      - "${RABBITMQ_PORT}:5672"
      - "${RABBITMQ_MANAGEMENT_PORT}:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "check_running"]
      interval: 10s
      timeout: 10s
      retries: 10
      start_period: 20s
    restart: unless-stopped

  minio:
    image: minio/minio:latest
    command: ["server", "/data", "--console-address", ":9001"]
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    ports:
      - "${MINIO_PORT}:9000"
      - "${MINIO_CONSOLE_PORT}:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  # Creates the buckets, then exits. It is idempotent and safe to re-run.
  minio-init:
    image: minio/mc:latest
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_LOGS_BUCKET: ${MINIO_LOGS_BUCKET}
      MINIO_ARTIFACTS_BUCKET: ${MINIO_ARTIFACTS_BUCKET}
    entrypoint: ["/bin/sh", "/bootstrap.sh"]
    volumes:
      - ./infra/minio/bootstrap.sh:/bootstrap.sh:ro
    depends_on:
      minio:
        condition: service_healthy
    restart: on-failure

  gateway:
    profiles: ["app"]
    build:
      context: ./gateway
      dockerfile: Dockerfile
    environment:
      ENVIRONMENT: ${ENVIRONMENT}
      GATEWAY_HOST: 0.0.0.0
      GATEWAY_PORT: 8080
      RUST_LOG: ${RUST_LOG}
      DATABASE_URL: postgres://buildlens_gateway:${GATEWAY_DB_PASSWORD}@postgres:5432/${POSTGRES_DB}
      DATABASE_MAX_CONNECTIONS: ${DATABASE_MAX_CONNECTIONS}
      DATABASE_CONNECT_TIMEOUT_SECONDS: ${DATABASE_CONNECT_TIMEOUT_SECONDS}
      REDIS_URL: redis://redis:6379
      GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID}
      GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET}
      GITHUB_REDIRECT_URI: ${GITHUB_REDIRECT_URI}
      GITHUB_API_BASE_URL: ${GITHUB_API_BASE_URL}
      GITHUB_WEBHOOK_URL: ${GITHUB_WEBHOOK_URL}
      GITHUB_WEBHOOK_SECRET: ${GITHUB_WEBHOOK_SECRET}
      TOKEN_ENCRYPTION_KEY: ${TOKEN_ENCRYPTION_KEY}
      SESSION_TTL_SECONDS: ${SESSION_TTL_SECONDS}
      RABBITMQ_URL: amqp://${RABBITMQ_DEFAULT_USER}:${RABBITMQ_DEFAULT_PASS}@rabbitmq:5672/${RABBITMQ_DEFAULT_VHOST}
      S3_ENDPOINT: http://minio:9000
      S3_REGION: us-east-1
      S3_ACCESS_KEY: ${MINIO_ROOT_USER}
      S3_SECRET_KEY: ${MINIO_ROOT_PASSWORD}
      S3_LOGS_BUCKET: ${MINIO_LOGS_BUCKET}
    ports:
      - "${GATEWAY_PORT}:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      minio:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    restart: unless-stopped

  analytics:
    profiles: ["app"]
    build:
      context: ./analytics
      dockerfile: Dockerfile
    environment:
      ANALYTICS_PORT: 8081
      ANALYTICS_DATABASE_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB}
      ANALYTICS_DB_PASSWORD: ${ANALYTICS_DB_PASSWORD}
      RABBITMQ_URL: amqp://${RABBITMQ_DEFAULT_USER}:${RABBITMQ_DEFAULT_PASS}@rabbitmq:5672/${RABBITMQ_DEFAULT_VHOST}
    ports:
      - "${ANALYTICS_PORT:-8081}:8081"
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost:8081/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    restart: unless-stopped

  ai-worker:
    profiles: ["app"]
    build:
      context: ./ai-worker
      dockerfile: Dockerfile
    environment:
      AI_PORT: 8082
      AI_DATABASE_URL: postgresql://buildlens_ai:${AI_DB_PASSWORD}@postgres:5432/${POSTGRES_DB}
      RABBITMQ_URL: amqp://${RABBITMQ_DEFAULT_USER}:${RABBITMQ_DEFAULT_PASS}@rabbitmq:5672/${RABBITMQ_DEFAULT_VHOST}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      AI_MANUAL_TRIGGER_TOKEN: ${AI_MANUAL_TRIGGER_TOKEN}
      AI_FAILURE_MODEL: ${AI_FAILURE_MODEL:-claude-opus-4-8}
      AI_SUMMARY_MODEL: ${AI_SUMMARY_MODEL:-claude-haiku-4-5}
      AI_MONTHLY_COST_CAP_USD: ${AI_MONTHLY_COST_CAP_USD:-10.00}
      AI_SUCCESS_SUMMARIES_ENABLED: ${AI_SUCCESS_SUMMARIES_ENABLED:-false}
      AI_SCHEDULED_REPORTS_ENABLED: ${AI_SCHEDULED_REPORTS_ENABLED:-false}
      S3_ENDPOINT: http://minio:9000
      S3_REGION: us-east-1
      S3_ACCESS_KEY: ${MINIO_ROOT_USER}
      S3_SECRET_KEY: ${MINIO_ROOT_PASSWORD}
      S3_LOGS_BUCKET: ${MINIO_LOGS_BUCKET}
    ports:
      - "${AI_PORT:-8082}:8082"
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      minio:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - >-
          import urllib.request;
          urllib.request.urlopen('http://localhost:8082/health', timeout=3)
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
  rabbitmq_data:
  minio_data:
